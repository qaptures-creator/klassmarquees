import type { Testimonial } from "@/types";

/**
 * Verified client testimonials only. No quotes have been supplied yet, so
 * this list is intentionally empty rather than filled with invented praise —
 * the Testimonials section renders a tasteful alternative until real,
 * attributed reviews are added here. See MEDIA_CHECKLIST.md.
 *
 * Add entries as: { quote, attribution, context }.
 */
export const testimonials: Testimonial[] = [];
